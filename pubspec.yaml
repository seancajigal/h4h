name: scam_shield
description: ScamShield — AI-powered scam detection via voice, text, and image.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  elevenlabs_agents: ^0.3.1
  permission_handler: ^11.3.1
  http: ^1.2.2
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true